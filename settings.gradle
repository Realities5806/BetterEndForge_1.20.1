pluginManagement {
    repositories {
        gradlePluginPortal()
        maven {
            name = 'MinecraftForge'
            url = 'https://maven.minecraftforge.net/'
        }
    }
}

plugins {
    // Commented out due to network restrictions in environment
    // id 'org.gradle.toolchains.foojay-resolver-convention' version '0.7.0'
}